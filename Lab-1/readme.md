# EventLoop

- JS is synchronous and single threaded bydefault

## There can be asynchronous behaviour

- with browser API - setTimeout, setInterval, setImmediate, nextTick
- with promises
- with event handlers

# promise 
- A function not executed immediately but it must be executed after a while it has some status during the execution at final it may resolve

# Callback
- that pass as argument or the parameter to another function .

## Modern js is divided in two types
- commonJs (.cjs) -> support OOPS
- ModuleJs (.mjs) -> follow modular approach -> important